# Load Nix flake environment
use flake

# Source all envrc.d scripts in order
for f in envrc.d/*.sh; do
  [ -f "$f" ] && source "$f"
done

# Local overrides (gitignored)
source_env_if_exists .envrc.local
